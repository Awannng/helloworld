import React from "react";
import MapTimeline from "../components/MapTimeline";

const TimeLinePage = () => {
    return (
        <div>
            <MapTimeline />
        </div>
    );
};

export default TimeLinePage;
